const BASE_URL = "https://astraltree.s3.us-east-2.amazonaws.com/media/"

const Logo = `${BASE_URL}Logo.png`

const nebulus = `${BASE_URL}nebulus.jpg`

const nebulus2 = `${BASE_URL}nebulus2.jpg`

const nebulus3 = `${BASE_URL}nebulus3.jpg`

const rocket = `${BASE_URL}rocket.svg`

const moon = `${BASE_URL}moon.svg`

const earth = `${BASE_URL}earth.svg`

const astronaut = `${BASE_URL}astronaut.svg`

const fourOfour = `${BASE_URL}404.svg`

export {
  Logo,
  nebulus,
  nebulus2,
  nebulus3,
  rocket,
  moon,
  earth,
  astronaut,
  fourOfour,
}
